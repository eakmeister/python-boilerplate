from distutils.core import setup

setup(name = 'boilerplate',
      version = '1.0',
      description = 'Automatic boilerplate generation for Python',
      author = 'Eric Kern',
      author_email = '',
      url = '',
      packages = ['boilerplate']
)

